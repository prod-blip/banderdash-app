export function setupCommand(): string {
  return [
    "Banderdash setup",
    "Status: MVP stub only.",
    "Next: PR 3+ will add real local configuration and preflight behavior."
  ].join("\n");
}
